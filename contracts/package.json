{
  "name": "@kleros/pnk-merkle-drop-contracts",
  "version": "0.1.0",
  "description": "Smart Contracts to manage and redeem PNK airdrops.",
  "main": "deployments",
  "author": "Kleros",
  "license": "MIT",
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat deploy",
    "deploy:arbitrum": "hardhat deploy --network arbitrum",
    "deploy:arbitrumSepolia": "hardhat deploy --network arbitrumSepolia",
    "export": "hardhat export --export-all deployments/merkleRedeem.ts"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "dotenv-safe": "^9.1.0",
    "hardhat": "^2.22.19",
    "hardhat-deploy": "^1.0.1",
    "hardhat-deploy-ethers": "^0.4.2"
  }
}
